# Contribution Guide

## 1. はじめに

Petrichor への改善提案・修正・開発参加ありがとうございます。

このドキュメントでは、開発をスムーズに進めるための基本ルールをまとめています。\
小さな改善から実験的な機能まで、幅広く歓迎します。

フォークで作った機能の Pull Request も大歓迎です。\
ニッチな機能、変わった機能、「これ誰が使うんだ？」というような機能でも、面白いアイデアがあれば気軽に送ってください。

AI を利用した実装でも問題ありません。\
最初から完璧に仕上げる必要はありません。

ただし、Iolite 全体の設計や統一感を保つため、Pull Request の内容について実装方法や UI / UX の変更をお願いしたり、マージ後に変更したりする場合があります。

すべての Pull Request がマージされるとは限りませんが、アイデアや実験そのものを歓迎します。

---

## 2. ブランチ運用

### 基本方針

変更は最終的に `main` へ集約します。

基本の流れ:

1. `main` から作業用ブランチを作成する
2. 作業用ブランチで変更する
3. Pull Request を `main` 向けに作成する

`main` は安定版として扱うため、直接 push せず、作業用ブランチを使用してください。

### ブランチ名

ブランチ名は、変更内容が簡潔に伝わる名前にしてください。

基本的には、変更の種類と対象を組み合わせます。

例:

- `docs/contributing`
- `feat/room-limit`
- `fix/ui-layout`
- `refactor/websocket`

厳密な命名規則ではありません。\
変更内容が分かる名前であれば問題ありません。

---

## 3. コミットメッセージ

コミットメッセージには Conventional Commits 形式を使用します。\
説明文は日本語で問題ありません。

例:

- `feat: 位置同期を追加`
- `fix: 切断時のクラッシュを修正`
- `docs: CONTRIBUTING.md を追加`
- `refactor: WebSocket処理を整理`
- `chore: CI設定を更新`

---

## 4. Pull Request

Pull Request には、変更内容が分かる説明を記載してください。

必要に応じて、以下の情報も記載してください。

- 変更理由
- 関連 Issue
- 動作確認方法

UI に変更がある場合は、画像の添付を推奨します。

通常の Pull Request の向き先は `main` です。

### 実験的な Pull Request

完成度が低い、実験段階である、UI が仮の状態である、といった理由だけで Pull Request をためらう必要はありません。

「こういう機能があったら面白そう」という段階の実装も歓迎します。

まだ作業途中の場合は、Draft Pull Request を使用しても構いません。

フォーク独自の機能についても、本流で使えそうなものや面白いものがあれば、ぜひ Pull Request を送ってください。

---

## 5. レビューとマージ

Pull Request は内容を確認したうえで `main` へマージされます。

開発初期は、方向性と品質を維持するため、最終的なマージ判断をメンテナーが行います。

機能そのものを歓迎していても、Iolite 全体との整合性を保つために、設計・実装・命名・UI / UX などの変更をお願いする場合があります。

また、マージされた機能についても、今後の開発に伴って仕様や UI / UX が変更されることがあります。

---

## 6. AI の利用

コードの作成、修正、調査などに AI ツールを利用しても構いません。

AI を使用したことだけを理由に Pull Request を拒否することはありません。

ただし、AI が生成したコードについても通常のコードと同様にレビューを行います。\
可能な範囲で、内容を確認し、動作確認を行ったうえで Pull Request を送ってください。

---

## 7. 品質チェック

可能な範囲で、Pull Request を作成する前に以下を実行してください。

```bash
make server-ci
make web-ci
```

すべてのチェックが成功することを確認してください。
